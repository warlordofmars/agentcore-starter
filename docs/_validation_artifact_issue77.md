# Validation artifact for issue #77

This file is intentional test noise created by the validation PR for the
mechanical scope-boundary check (issue #77). It exists solely to make the
validation PR's diff stray outside the linked issue's `Files to touch`
section, which should cause the new `agent-safe scope check` CI workflow
to FAIL.

The validation PR is closed without merging — this file should never
land on `development`. If you are reading this on a long-lived branch,
something has gone wrong.
